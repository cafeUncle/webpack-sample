// Greeter.js  conmonJs 规范，require/exports
var config = require("./config.json")

module.exports = function() {
  var greet = document.createElement('div');
  greet.textContent = config.greetText;
  console.log(1)
  return greet;
};